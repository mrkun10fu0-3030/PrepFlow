<% content_for :title, "仕込み登録" %>

<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow-sm">
        <div class="card-body p-4">
          <h1 class="fw-bold mb-4" style="color: #2d6a4f;">仕込み登録</h1>
          <%= render "form", prep_task: @prep_task %>
          <div class="mt-3">
            <%= link_to "リストへ戻る", prep_tasks_path, class: "btn btn-outline-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>